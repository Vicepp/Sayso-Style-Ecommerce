# Files that live in the repo but are not part of the theme.
# Keeps `shopify theme package`, `theme push` and `theme dev` clean, and
# keeps the import zip to exactly what Shopify expects.

preview/
scripts/
.github/
.git/
node_modules/

README.md
.gitattributes
.gitignore
.theme-check.yml
.shopifyignore
